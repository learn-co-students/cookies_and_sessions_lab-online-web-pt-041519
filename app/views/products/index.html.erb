<h3>Items in cart:</h3>
<% cart.each do |item| %>
    <%= item %>
<% end %><br>

<%= form_tag('/add') do %>
    <%= label_tag(:product, "Item to add:") %>
    <%= text_field_tag(:product) %><br><br>
    <%= submit_tag("add to cart") %>
<% end %>